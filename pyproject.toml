[tool.coverage.run]
branch = true
source = [ "sceptre_cdk_handler" ]

[tool.coverage.report]
exclude_lines = [ ]
show_missing = true

[tool.pytest.ini_options]
addopts = "--cov --cov-report term-missing"
